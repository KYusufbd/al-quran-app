import React from 'react';

const Loading = () => {
  return (
    <div className="flex w-full h-screen bg-base-100 justify-center items-center">
      <span className="loading loading-bars text-primary loading-xl"></span>
    </div>
  );
};

export default Loading;
